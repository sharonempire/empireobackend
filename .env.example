# ─── Database (local Postgres via docker-compose) ───────────────────────────
# Use the local Postgres container started by docker-compose.yml.
# When running in Docker the hostname is `postgres` (service name).
# Copy this file to `.env` before running `docker compose up` and change secrets as needed.
DATABASE_URL=postgresql+asyncpg://empireo:empireo_local@postgres:5432/empireo
DATABASE_URL_SYNC=postgresql://empireo:empireo_local@postgres:5432/empireo

# ─── Redis ──────────────────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ─── JWT ────────────────────────────────────────────────────────────────────
JWT_SECRET_KEY=change-me-to-a-random-secret-at-least-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480
REFRESH_TOKEN_EXPIRE_DAYS=30

# ─── App ────────────────────────────────────────────────────────────────────
APP_NAME=Empireo Brain
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# ─── CORS ───────────────────────────────────────────────────────────────────
CORS_ORIGINS=["*"]

# ─── OpenAI (optional for local dev) ────────────────────────────────────────
OPENAI_API_KEY=

# ─── AWS S3 (optional for local dev) ────────────────────────────────────────
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=
AWS_REGION=ap-south-1

# NOTE: This project uses a local Postgres container for development. Avoid
# pointing DATABASE_URL* at external managed DBs (e.g., Supabase) in your
# local `.env`. For production, provide secure credentials via your CI/CD
# or secret manager and do not commit them to the repository.
